/**
 * Twitter Card
 *
 * The same artwork as the Open Graph card. X falls back to `og:image` on its own, but
 * naming the image explicitly is what makes the large summary card render reliably.
 */

export { default, alt, size, contentType } from './opengraph-image';
